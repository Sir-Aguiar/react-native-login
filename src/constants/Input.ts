import { StyleSheet } from "react-native";

const { DefaultInput } = StyleSheet.create({
  DefaultInput: {
    padding: 12,
    borderWidth: 1,
    borderColor: "#222",
    borderRadius: 4,
  },
});

export { DefaultInput };
